'use strict'

const expect = require('chai').expect
const validateTags = require('../validateTags')

describe( 'validateTags', function() {
  [
    "Richard",
    "rhyatt"
  ].forEach( function( validateTags ) {
    expect(err).to.not.exist
  })
}